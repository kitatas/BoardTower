namespace BoardTower.Common.Application
{
    public enum SceneName
    {
        None,
        Game,
    }

    public enum LoadType
    {
        None,
        Direct,
        Fade,
    }

    public enum Fade
    {
        None,
        In,
        Out,
    }
}